#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    long long chon = (long long)n * (n - 1) * (n - 2) * (n - 3) * (n - 4) / 120;
    long long ket_qua = chon * chon * 120;

    printf("%lld\n", ket_qua);
    return 0;
}

