#include <stdio.h>

int n;
long long m;
int perm[51];

int main() {
    scanf("%d %lld", &n, &m);

    int left = 0, right = n - 1;
    int start = 1, end = n;

    while (start <= end) {
        if (start == end) {
            perm[left] = start;
            break;
        }

        long long total = 1;
        for (int i = 1; i <= end - start; i++) total *= i;

        if (m <= total) {
            perm[left++] = end;
        } else {
            perm[right--] = end;
            m -= total;
        }
        end--;
    }

    for (int i = 0; i < n; i++)
        printf("%d ", perm[i]);
    printf("\n");

    return 0;
}

