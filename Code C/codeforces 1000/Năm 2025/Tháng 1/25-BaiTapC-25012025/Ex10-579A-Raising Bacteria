#include <stdio.h>

int main() {
    int x;
    scanf("%d", &x);
    
    int count = 0;
    
    while (x) {
        if (x & 1) {
            count++;
        }
        x >>= 1;
    }
    
    printf("%d\n", count);
    return 0;
}

