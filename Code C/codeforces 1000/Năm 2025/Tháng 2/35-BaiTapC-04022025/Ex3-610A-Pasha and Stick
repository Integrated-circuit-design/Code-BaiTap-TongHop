#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    if (n % 2 != 0) {
        printf("0\n");
    } else {
        int half = n / 2;
        int result = half / 2 - (half % 2 == 0);
        printf("%d\n", result);
    }

    return 0;
}

