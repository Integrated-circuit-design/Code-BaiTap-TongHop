#include <stdio.h>
#include <math.h>

int main() {
    long long a, b, s;
    scanf("%lld %lld %lld", &a, &b, &s);
    long long min_steps = abs(a) + abs(b);
    if (s >= min_steps && (s - min_steps) % 2 == 0) {
        printf("Yes\n");
    } else {
        printf("No\n");
    }
    return 0;
}

