#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        if (i % 3 == 0)
            printf("a");
        else if (i % 3 == 1)
            printf("b");
        else
            printf("c");
    }

    printf("\n");
    return 0;
}

