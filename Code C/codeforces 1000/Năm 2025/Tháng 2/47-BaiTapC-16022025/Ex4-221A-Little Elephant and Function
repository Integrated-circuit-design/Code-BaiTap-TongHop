#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    
    int arr[n];
    arr[0] = n;

    for (int i = 1; i < n; i++) {
        arr[i] = i;
    }

    for (int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}

