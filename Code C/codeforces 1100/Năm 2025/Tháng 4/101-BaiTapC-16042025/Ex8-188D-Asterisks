#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    char stars[51];

    for (int i = 1; i <= n; i++) {
        stars[i] = '\0';
        for (int j = 0; j < i; j++) {
            stars[j] = '*';
        }
        printf("%s\n", stars);
    }

    return 0;
}

