#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    if (n == 3) {
        printf("1\n");
    } else {
        int jumps = 0;

        for (int i = 4; i <= n; i++) {
            jumps += (i - 3);
        }

        printf("%d\n", jumps);
    }

    return 0;
}

