#include <stdio.h>

int main() {
    int n, i;
    long long result = 0, power = 2;

    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        result += power;
        power *= 2;
    }

    printf("%lld\n", result);

    return 0;
}

