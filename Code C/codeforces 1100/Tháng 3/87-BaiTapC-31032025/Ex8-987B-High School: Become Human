#include <stdio.h>
#include <math.h>

int main() {
    int x, y;
    scanf("%d %d", &x, &y);

    double lhs = y * log(x);
    double rhs = x * log(y);

    if (lhs < rhs)
        printf("<\n");
    else if (lhs > rhs)
        printf(">\n");
    else
        printf("=\n");

    return 0;
}

