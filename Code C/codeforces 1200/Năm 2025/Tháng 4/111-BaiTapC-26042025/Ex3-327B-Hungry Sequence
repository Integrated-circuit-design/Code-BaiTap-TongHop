#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int start = 3;

    for (int i = 0; i < n; i++) {
        printf("%d ", start + i * 3);
    }
    
    printf("\n");

    return 0;
}

