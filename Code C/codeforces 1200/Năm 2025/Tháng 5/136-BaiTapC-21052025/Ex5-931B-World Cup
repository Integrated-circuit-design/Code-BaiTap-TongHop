#include <stdio.h>

int main() {
    int N, sum = 0;
    printf("Nhập vào một số nguyên N: ");
    scanf("%d", &N);

    for (int i = 1; i <= N; i++) {
        sum += i;
    }

    printf("Tổng các số từ 1 đến %d là: %d\n", N, sum);

    return 0;
}

