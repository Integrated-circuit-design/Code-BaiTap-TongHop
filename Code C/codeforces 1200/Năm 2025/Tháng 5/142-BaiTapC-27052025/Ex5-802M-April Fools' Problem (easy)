#include <stdio.h>

int main() {
    int n, k;
    scanf("%d %d", &n, &k);
    
    int arr[2200];
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int sum = 0;
    for (int i = 0; i < k; i++) {
        sum += arr[i];
    }

    printf("%d\n", sum);
    
    return 0;
}

