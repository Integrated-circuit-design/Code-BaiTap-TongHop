#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    
    int max_teams = (n + m) / 3;
    if (max_teams > n) {
        max_teams = n;
    }
    if (max_teams > m) {
        max_teams = m;
    }
    
    printf("%d\n", max_teams);
    
    return 0;
}

