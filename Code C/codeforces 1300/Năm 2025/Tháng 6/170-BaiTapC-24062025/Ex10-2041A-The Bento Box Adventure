#include <stdio.h>

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);

    int total = 1 + 2 + 3 + 4 + 5;
    int sum = a + b + c + d;
    int remaining = total - sum;
    
    printf("%d\n", remaining);
    return 0;
}

