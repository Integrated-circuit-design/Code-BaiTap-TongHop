#include <stdio.h>

int main() {
    int n, m;
    scanf("%d %d", &n, &m);

    int k = (n < m ? n : m) + 1;
    printf("%d\n", k);

    for (int i = 0; i < k; i++) {
        printf("%d %d\n", n - i, i);
    }

    return 0;
}

