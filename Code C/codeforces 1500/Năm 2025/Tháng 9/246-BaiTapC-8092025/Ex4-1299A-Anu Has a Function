#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    long long a[n];
    long long OR_all = 0;

    for (int i = 0; i < n; i++) {
        scanf("%lld", &a[i]);
        OR_all |= a[i];
    }

    int pos = 0;
    long long max_contrib = -1;

    for (int i = 0; i < n; i++) {
        long long others = OR_all ^ a[i];
        long long unique_bits = a[i] & (~others);
        if (unique_bits > max_contrib) {
            max_contrib = unique_bits;
            pos = i;
        }
    }

    for (int i = pos; i < n; i++) {
        printf("%lld ", a[i]);
    }
    for (int i = 0; i < pos; i++) {
        printf("%lld ", a[i]);
    }
    printf("\n");

    return 0;
}

