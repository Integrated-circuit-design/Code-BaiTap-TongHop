#include <stdio.h>
#include <stdbool.h>

#define MAXM 300000

int main() {
    int n, m;
    scanf("%d %d", &n, &m);

    int a[MAXM], b[MAXM];
    for (int i = 0; i < m; i++) {
        scanf("%d %d", &a[i], &b[i]);
    }

    int firstA = a[0], firstB = b[0];
    bool possible = false;

    for (int xTry = 0; xTry < 2 && !possible; xTry++) {
        int x = (xTry == 0) ? firstA : firstB;

        int y = -1;
        bool fail = false;

        for (int i = 0; i < m; i++) {
            if (a[i] != x && b[i] != x) {
                if (y == -1) {
                    y = a[i];
                } else if (y != a[i] && y != b[i]) {
                    fail = true;
                    break;
                }
            }
        }

        if (!fail) {
            possible = true;
        }
    }

    if (possible) {
        printf("YES\n");
    } else {
        printf("NO\n");
    }

    return 0;
}

