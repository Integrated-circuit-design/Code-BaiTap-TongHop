#include <stdio.h>

int main() {
    int x, y;

    if (scanf("%d %d", &x, &y) != 2) {
        printf("Nhap khong hop le!\n");
        return 1;
    }

    int sum = 0;
    sum = x;
    sum += y;

    printf("%d\n", sum);

    return 0;
}

