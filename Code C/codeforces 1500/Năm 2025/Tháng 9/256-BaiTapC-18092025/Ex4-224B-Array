#include <stdio.h>
#include <stdlib.h>

#define MAXN 100005

int main() {
    int n, k;
    scanf("%d %d", &n, &k);

    int a[MAXN];
    int freq[MAXN] = {0};
    for (int i = 0; i < n; i++) scanf("%d", &a[i]);

    int l = 0;
    int distinct = 0;
    int ans_l = -1, ans_r = -1;

    for (int r = 0; r < n; r++) {
        if (freq[a[r]] == 0) distinct++;
        freq[a[r]]++;

        while (distinct > k) {
            freq[a[l]]--;
            if (freq[a[l]] == 0) distinct--;
            l++;
        }

        if (distinct == k) {
            while (freq[a[l]] > 1) {
                freq[a[l]]--;
                l++;
            }
            ans_l = l + 1;
            ans_r = r + 1;
            break;
        }
    }

    if (ans_l == -1)
        printf("-1 -1\n");
    else
        printf("%d %d\n", ans_l, ans_r);

    return 0;
}

