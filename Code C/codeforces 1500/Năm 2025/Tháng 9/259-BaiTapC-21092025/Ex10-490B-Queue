#include <stdio.h>

#define MAXN 200005
#define MAXID 1000006

typedef struct {
    int prev;
    int next;
} Student;

Student stu[MAXID];
int used[MAXID];
int idList[MAXN];

int main() {
    int n;
    scanf("%d", &n);

    for (int i = 0; i < n; i++) {
        int a, b;
        scanf("%d %d", &a, &b);
        idList[i] = i + 1;
        stu[i + 1].prev = a;
        stu[i + 1].next = b;
        used[i + 1] = 1;
    }

    int start = -1;
    for (int i = 1; i <= n; i++) {
        if (stu[i].prev == 0) {
            start = i;
            break;
        }
    }

    int cur = start;
    while (cur != 0) {
        printf("%d ", cur);
        cur = stu[cur].next;
    }
    printf("\n");

    return 0;
}

