#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int n;
        scanf("%d", &n);

        int a[100005];
        for (int i = 0; i < n; i++)
            scanf("%d", &a[i]);

        int pos = -1;
        for (int i = n - 2; i >= 0; i--) {
            if (a[i] > a[i + 1]) {
                pos = i;
                break;
            }
        }

        if (pos == -1) {
            for (int i = 0; i < n; i++) {
                printf("%d ", a[i]);
            }
            printf("\n");
            continue;
        }

        for (int i = pos + 1; i < n; i++)
            printf("%d ", a[i]);

        for (int i = 0; i <= pos; i++)
            printf("%d ", a[i]);

        printf("\n");
    }

    return 0;
}

