#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    for (int case_id = 1; case_id <= t; ++case_id) {
        long long n, m, r, c;
        scanf("%lld %lld %lld %lld", &n, &m, &r, &c);
        printf("%lld %lld\n", r, c);
    }

    return 0;
}
