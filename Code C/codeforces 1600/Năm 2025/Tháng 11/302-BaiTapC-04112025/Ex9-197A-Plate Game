#include <stdio.h>

int main() {
    int a, b, r;
    scanf("%d %d %d", &a, &b, &r);

    int diameter = 2 * r;

    if (a >= diameter && b >= diameter) {
        printf("First\n");
    } else {
        printf("Second\n");
    }

    return 0;
}

