#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int factor = 2;

    while (n > 1) {
        if (n % factor == 0) {
            printf("%d ", factor);
            n = n / factor;
        } else {
            factor++;
        }
    }

    return 0;
}

