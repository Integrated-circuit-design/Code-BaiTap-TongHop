#include <stdio.h>
#include <string.h>

#define MAX_N 100005

int str_to_int(char *s) {
    int val = 0;
    for (int i = 0; s[i]; i++) {
        val = val * 27 + (s[i]-'a'+1);
    }
    return val;
}

int is_palindrome(char *s) {
    int len = strlen(s);
    if (len == 1) return 1;
    if (len == 2) return s[0] == s[1];
    if (len == 3) return s[0] == s[2];
    return 0;
}

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int n;
        scanf("%d", &n);

        char s[4];
        int found = 0;
        int seen[19684] = {0};

        for (int i = 0; i < n; i++) {
            scanf("%s", s);

            if (found) continue;

            if (is_palindrome(s)) {
                found = 1;
                continue;
            }

            int len = strlen(s);
            int rev_val = 0;

            for (int j = len-1; j >= 0; j--) {
                rev_val = rev_val * 27 + (s[j]-'a'+1);
            }

            if (seen[rev_val]) {
                found = 1;
                continue;
            }

            int val = str_to_int(s);
            seen[val] = 1;
        }

        if (found) printf("YES\n");
        else printf("NO\n");
    }
    return 0;
}

