#include <stdio.h>

int interesting_count(int n) {
    return n / 10;
}

int main() {
    int t;
    scanf("%d", &t);

    for (int i = 0; i < t; i++) {
        int n;
        scanf("%d", &n);
        int result = (n >= 9) ? (n / 10) + 1 : 0;
        printf("%d\n", result);
    }

    return 0;
}

