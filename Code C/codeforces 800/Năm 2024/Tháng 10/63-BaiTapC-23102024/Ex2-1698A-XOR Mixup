#include <stdio.h>

int main() {
    int test_cases;
    scanf("%d", &test_cases);

    for (int case_index = 0; case_index < test_cases; case_index++) {
        int length;
        scanf("%d", &length);
        int arr[length];
        int xor_result = 0;

        for (int i = 0; i < length; i++) {
            scanf("%d", &arr[i]);
            xor_result ^= arr[i];
        }

        printf("%d\n", xor_result);
    }

    return 0;
}

