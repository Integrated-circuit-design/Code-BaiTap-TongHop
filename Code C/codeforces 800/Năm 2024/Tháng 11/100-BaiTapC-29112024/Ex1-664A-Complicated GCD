#include <stdio.h>
#include <string.h>

int main() {
    char a[102], b[102];

    scanf("%s %s", a, b);

    if (strlen(a) == strlen(b) && strcmp(a, b) == 0) {
        printf("%s\n", a);
    } else {
        printf("1\n");
    }

    return 0;
}

