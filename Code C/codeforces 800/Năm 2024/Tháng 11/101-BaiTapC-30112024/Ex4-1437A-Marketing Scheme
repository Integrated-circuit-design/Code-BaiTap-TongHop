#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        long long l, r;
        scanf("%lld %lld", &l, &r);

        if (l * 2 > r) {
            printf("YES\n");
        } else {
            printf("NO\n");
        }
    }

    return 0;
}

