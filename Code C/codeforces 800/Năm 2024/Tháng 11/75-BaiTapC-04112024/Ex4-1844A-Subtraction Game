#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int a, b;
        scanf("%d %d", &a, &b);
        
        int n = a + 1;
        
        printf("%d\n", n);
    }

    return 0;
}

