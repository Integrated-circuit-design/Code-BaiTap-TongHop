#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int a;
    for (int i = 0; i < n; i++) {
        scanf("%d", &a);
        if (a % 2 == 0) {
            printf("%d ", a + 1);
        } else {
            printf("%d ", a - 1);
        }
    }

    return 0;
}

