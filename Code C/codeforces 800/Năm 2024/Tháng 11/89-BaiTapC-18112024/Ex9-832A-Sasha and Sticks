#include <stdio.h>

int main() {
    long long n, k;
    scanf("%lld %lld", &n, &k);
    long long moves = n / k;
    if (moves % 2 == 1) {
        printf("YES\n");
    } else {
        printf("NO\n");
    }
    return 0;
}

