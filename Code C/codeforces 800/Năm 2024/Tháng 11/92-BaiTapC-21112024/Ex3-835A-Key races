#include <stdio.h>

int main() {
    int s, v1, v2, t1, t2;
    scanf("%d %d %d %d %d", &s, &v1, &v2, &t1, &t2);

    int total_time_first = (s * v1) + 2 * t1;
    int total_time_second = (s * v2) + 2 * t2;

    if (total_time_first < total_time_second) {
        printf("First\n");
    } else if (total_time_first > total_time_second) {
        printf("Second\n");
    } else {
        printf("Friendship\n");
    }

    return 0;
}

