#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int l, r;
        scanf("%d %d", &l, &r);

        if (l + 1 <= r) {
            printf("%d %d\n", l, l + 1);
        }
    }

    return 0;
}

