#include <stdio.h>

int main() {
    int n, m, z;
    scanf("%d %d %d", &n, &m, &z);
    
    int count = 0;
    
    for (int i = 1; i <= z; i++) {
        if (i % n == 0 && i % m == 0) {
            count++;
        }
    }

    printf("%d\n", count);

    return 0;
}

