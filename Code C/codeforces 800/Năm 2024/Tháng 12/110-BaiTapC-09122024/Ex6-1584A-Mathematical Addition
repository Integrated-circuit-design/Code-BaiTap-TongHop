#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);
    
    while (t--) {
        long long u, v;
        scanf("%lld %lld", &u, &v);
        
        long long x = u;
        long long y = -v;
        
        printf("%lld %lld\n", x, y);
    }
    
    return 0;
}

