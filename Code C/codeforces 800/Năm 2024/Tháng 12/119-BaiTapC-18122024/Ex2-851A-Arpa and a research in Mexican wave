#include <stdio.h>

int main() {
    long long n, k, t;

    scanf("%lld %lld %lld", &n, &k, &t);

    if (t <= k) {
        printf("%lld\n", t);
    }
    else if (t <= n) {
        printf("%lld\n", k);
    }
    else {
        printf("%lld\n", k - (t - n));
    }

    return 0;
}

