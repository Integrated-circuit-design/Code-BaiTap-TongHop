#include <stdio.h>
#include <ctype.h>

int main() {
    char word[104];
    scanf("%s", word);
    if (word[0] >= 'a' && word[0] <= 'z') {
        word[0] = word[0] - ('a' - 'A');
    }
    printf("%s\n", word);
    return 0;
}
