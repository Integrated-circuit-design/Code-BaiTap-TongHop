#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    if (n == 1) {
        printf("-1\n");
    } else {
        for (int i = n; i >= 1; i--) {
            printf("%d ", i);
        }
        printf("\n");
    }

    return 0;
}

